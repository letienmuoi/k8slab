apiVersion: v1
kind: Service
metadata:
  name: {{ include "qnet-quality.fullname" . }}
  labels:
    {{- include "qnet-quality.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "qnet-quality.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
