apiVersion: batch/v1
kind: Job
metadata:
  name: qnet-smoke-test
  namespace: qnet-capstone
  labels:
    app.kubernetes.io/name: smoke-test
    app.kubernetes.io/instance: qnet-capstone
    app.kubernetes.io/part-of: qnet-data-quality
    app.kubernetes.io/component: test
spec:
  backoffLimit: 1
  activeDeadlineSeconds: 120
  ttlSecondsAfterFinished: 900
  template:
    metadata:
      labels:
        app.kubernetes.io/name: smoke-test
        app.kubernetes.io/instance: qnet-capstone
        app.kubernetes.io/part-of: qnet-data-quality
        app.kubernetes.io/component: test
        role: access-client
    spec:
      restartPolicy: Never
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: smoke
          image: mualanhlung017/qnet-gateway:1.0.0
          imagePullPolicy: IfNotPresent
          command:
            - python
            - /app/smoke_test.py
          env:
            - name: GATEWAY_URL
              value: http://qnet-gateway:8080
            - name: PUBLIC_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: qnet-secrets
                  key: public-api-token
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 150m
              memory: 96Mi
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 8Mi
